[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: integration tests (may touch the filesystem or multiple components)
    live: requires real API credentials; skipped in CI (set RUN_LIVE=1 to enable)
