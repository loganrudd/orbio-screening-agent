# Core
pydantic>=2.6
structlog>=24.1

# LLM
anthropic>=0.40

# Voice (STT + TTS) — decoupled from the LLM
deepgram-sdk>=3.7
# Optional TTS upgrade:
# elevenlabs>=1.0

# Observability (optional; tracing is a no-op if absent)
mlflow>=3.0

# Bonus / stretch
qdrant-client>=1.9       # RAG FAQ (bonus)
fastapi>=0.110           # web UI (stretch)
uvicorn>=0.29            # web UI (stretch)

# Dev
pytest>=8.0
pytest-asyncio>=0.23
